// Plantilla. Copia a secrets.h y rellena. secrets.h esta en .gitignore.
#pragma once

#define SECRETS_WIFI_SSID     "TU_SSID"
#define SECRETS_WIFI_PASSWORD "TU_PASSWORD"

#define SECRETS_MQTT_HOST     "192.168.0.100"
#define SECRETS_MQTT_PORT     1883
#define SECRETS_MQTT_USER     "smartlabs"
#define SECRETS_MQTT_PASS     "smartlabs_mqtt_2024"

// Opcional: si esta definida, ArduinoOTA pedira password al subir por aire.
// Para descubrir la estacion en la red local:
//   pio run -e prestamo -t upload --upload-port esp32_SMART10003.local
// (mDNS expone <hostname>.local; el hostname es el serial number).
//#define SECRETS_OTA_PASSWORD  "ota_pass_aqui"
